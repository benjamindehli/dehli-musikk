const initialState = {
    no: {
      name: 'norsk',
      path: ''
    },
    en: {
      name: 'english',
      path: 'en/'
    }
  };
  
  const reducer = (state = initialState) => {
    return state;
  };
  
  export default reducer;
  